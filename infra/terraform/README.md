# Terraform (placeholder)

Modules:

- `modules/networking` — VPC, subnets
- `modules/rds` — PostgreSQL
- `modules/elasticache` — Redis
- `modules/elasticsearch` — OpenSearch / Elastic Cloud
- `modules/eks` or `modules/aks` — container platform

State: remote backend (S3 + DynamoDB or Azure Storage).
